class TaskOptionModel {
    constructor(id, title, icon, isSet) {
        this.id = id,
            this.title = title,
            this.icon = icon,
            this.isSet = isSet
    }
}

export default TaskOptionModel;