class TaskOptionModel {
    constructor(id, title, icon, isSet, data, label) {
        this.id = id,
            this.title = title,
            this.icon = icon,
            this.isSet = isSet,
            this.data = data,
            this.label = label
    }
}

export default TaskOptionModel;